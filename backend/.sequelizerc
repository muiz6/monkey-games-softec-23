const path = require("path");
module.exports = {
  config: path.resolve("lib/db/config", "database.json"),
  "models-path": path.resolve("lib/db", "model"),
  "seeders-path": path.resolve("lib/db", "seeders"),
  "migrations-path": path.resolve("lib/db", "migrations"),
};